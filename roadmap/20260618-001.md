# Feature: [Receipt image analyzer - multiple transactions in one image]

## Requirements
- Improvement from `20260616-001` but the image may contains one or multiple transactions.
  - Example: Screenshot of transaction history of an e-commerce.
- Still the same endpoint, but it now returns list of transactions.
  - If only 1 transaction in an image, e.g. a receipt, just return single transaction within the list.
  - Else, return multiple.
